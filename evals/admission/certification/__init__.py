"""#162D certification machinery (issue #176).

Subpackage of the admission evaluation tools; production code must not import
it. The frozen #162A ``runner_digest()`` globs only top-level
``evals/admission/*.py``, so everything here leaves accepted baseline
artifacts byte-valid.
"""
