.git
.venv
.mypy_cache
.pytest_cache
.ruff_cache
__pycache__
.agents
.codex
.env
.env.*
# .env.example is a committed template (no real secrets — only commented-out
# defaults) and MUST ship in the CI image so the ci-contract test that asserts
# the dark-write settings are documented can read it.
!.env.example

# Hosted-platform Node build artifacts (not the manifests — those are needed by
# the Portal/Control-Plane Dockerfiles which share this repo-root context).
node_modules
.next
apps/portal/.next
packages/*/dist
test-results
playwright-report
.pnpm-store
